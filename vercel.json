{
    "installCommand": "npm install -g yarn@4.1.1 && yarn install",
    "buildCommand": "corepack enable && corepack prepare yarn@4.5.3 --activate && yarn install && yarn workspace @safe-global/web build",
    "outputDirectory": "apps/web/.next"
}