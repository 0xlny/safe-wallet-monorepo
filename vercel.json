{
    "installCommand": "corepack enable && corepack prepare yarn@4.0.2 --activate && yarn install",
    "buildCommand": "yarn workspace @safe-global/web build",
    "outputDirectory": "apps/web/.next"
}